import 'package:flutter/material.dart';

class HomeTabEnergy extends StatelessWidget {
  const HomeTabEnergy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
