// 🐦 Flutter imports:
import 'package:flutter/material.dart';

/// The second tab displayed in home screen
class HomeTabEnergy extends StatelessWidget {
  /// Display information about the energy consimed by the devices
  const HomeTabEnergy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
