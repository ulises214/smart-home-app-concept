part of smart_devices_controllers;

/// Creates a unique controller for a [SmartSpotlight] object
class SmartSpotlightController extends SmartDeviceController {
  /// Initialize the smart ac
  SmartSpotlightController(SmartSpotlight ac) : super(ac);
}
